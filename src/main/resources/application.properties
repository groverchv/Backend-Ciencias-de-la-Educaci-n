spring.application.name=Ciencias-de-la-Educacion

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ciencias-de-la-educacion
spring.datasource.username=postgres
spring.datasource.password=muerte
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret.key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration.ms=1800000
# 1800000 ms = 30 minutos (Access Token)
jwt.refresh.expiration.ms=604800000
# 604800000 ms = 7 días (Refresh Token)

# CORS Configuration
cors.allowed.origins=*
cors.allowed.methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
cors.allowed.headers=*
cors.exposed.headers=Authorization
cors.allow.credentials=true
cors.max.age=3600

# Backup Configuration
backup.directory=./backups
backup.retention.days=30
# Retención de backups: eliminar automáticamente backups con más de 30 días
